<?xml version="1.0" encoding="utf-8"?>
<link.zhidou.translator.ui.view.CommonActionBar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/common_action_bar"
    android:layout_width="match_parent"
    android:layout_height="@dimen/action_bar_height"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/back"
            android:layout_gravity="center_vertical"
            android:layout_width="@dimen/action_bar_height"
            android:layout_height="match_parent"
            android:scaleType="center"
            android:src="@drawable/back_icon_selector"
            />

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:visibility="invisible"
            android:gravity="center"
            android:textSize="26dp" />

        <ImageView
            android:id="@+id/settings"
            android:layout_gravity="center_vertical"
            android:layout_width="@dimen/action_bar_height"
            android:layout_height="match_parent"
            android:scaleType="center"
            android:visibility="invisible"
            android:src="@drawable/more_icon_selector"
            />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_alignParentBottom="true"
        android:background="#EBEBEB"
        />

</link.zhidou.translator.ui.view.CommonActionBar>